export interface HeaderProps {
  copy: {
    anchor: string
    button: string
    decoratedTitle: string
    description: string
    label: string
    stats: {
      title: string
      description: string
    }[]
    title: string
  }
}
